CREATE DATABASE my_cart;
USE my_cart;
CREATE TABLE Cart_det (
    Id INT PRIMARY KEY,
    Title VARCHAR(255),
    Price DECIMAL(10, 2),
    IMAGEURL VARCHAR(255)  -- Path to the image
);






























